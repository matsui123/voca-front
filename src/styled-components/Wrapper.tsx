import styled from 'styled-components'

const Wrapper = styled.section`
    display: inline;
    margin-right: auto;
    margin-left: auto;
    max-width: 800px;
    text-align: center;
`;

export default Wrapper;
